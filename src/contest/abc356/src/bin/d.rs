use proconio::input;
// use proconio::marker::Chars;
// use itertools::Itertools;

const MOD: i64 = 998244353;

fn count(n: i64, i: i32) -> usize {}

fn main() {
    input! {
        n: i64,
        m: i64,
    }

    let mut ans: i128 = 0;

    for i in 0..61 {
        m_i_bidigit = (m >> i) & 1;
        if m_i_digit == 0 {
            continue;
        }
    }
}
